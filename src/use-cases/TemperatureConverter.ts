type dataToConvert = {
    temperature: number,
    scale: string,
    scaleToConvert: string,
}

export class TemperatureConverter {
    static execute({temperature, scale, scaleToConvert}: dataToConvert) {

    }
}